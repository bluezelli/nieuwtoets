{% extends 'base.html.twig' %}

{% block title %}Hello LandController!{% endblock %}

{% block body %}
<style>

</style>

    <div class="container">
        <div class="row">
            <div class="col">
                <table class="table">
                    <thead>
                    <tr>
                        <th scope="col">Continent naam</th>
                        <th scope="col">landen</th>
{#                        <th scope="col">title</th>#}
                    </tr>

                    </thead>
                    <tbody>
                    {% for continents in continent %}
                        <tr>
                            <td>
                               {{ continents.name }}

                            </td>
                            <td>
                                <a class="btn btn-primary" href="{{ path('land',{id: continents.id}) }}">Landen</a>
                            </td>
                            <td>
                                                            <a class="btn btn-warning" href="{{ path('update',{id: continents.id}) }}">update</a>
                            </td>
                        </tr>

                    {% endfor %}
                    </tbody>
                </table>
            </div>
            {#            <a class="btn btn-dark" href="{{ path('insert') }}"></a>#}
        </div>
    </div>
{% endblock %}
